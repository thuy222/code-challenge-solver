NODE_ENV=development
PORT=3000
DB_HOST=localhost
DB_PORT=5432
# Host-side port for the Postgres container in docker-compose.
# Override if 5432 is already taken on your host.
DB_HOST_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=users_db
